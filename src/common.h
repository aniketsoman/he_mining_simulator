#pragma once

// constants
const size_t MINUTES_IN_HOUR = 60;
const size_t DEFAULT_SPEED = 60; // default speed : 60 should run 72 hours in 72 minutes
const size_t DEFAULT_SIMULATION_TIME = 72;  // 72 hours
const size_t TRANSIT_TIME = 30; // 30 minutes
const size_t UNLOAD_TIME = 5; // 5 minutes
const size_t MAX_MINING_TIME = 5; // 5 hours